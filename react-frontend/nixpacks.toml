# Nixpacks configuration for Railway deployment
# Prevents platform-specific dependency issues from Windows-generated package-lock.json

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Remove Windows-generated package-lock.json to avoid platform conflicts
# Then install fresh - rollup needs platform-specific optional deps
cmds = ["rm -f package-lock.json", "npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l 3000"
